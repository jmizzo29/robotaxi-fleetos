export default function RoboWordmark({ className = '', colorClass = 'text-[#172231]' }) {
  return (
    <span className={`font-bold uppercase ${colorClass} ${className}`}>
      ROBOAGENT
    </span>
  );
}
