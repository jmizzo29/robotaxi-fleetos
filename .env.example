VITE_MAPBOX_TOKEN=your_mapbox_public_token

# Optional. Leave empty on Vercel unless you have deployed the Express backend publicly.
# Local development defaults to http://localhost:3001/api.
VITE_TESLA_API_BASE=

# AI runs through /api/ai/analyze; provider API keys belong in backend/.env or Vercel env vars.
